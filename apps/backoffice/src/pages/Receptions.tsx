import { PageStub } from '../components/PageStub';
import { BookOpen } from 'lucide-react';

export function ReceptionsPage() {
  return <PageStub title="Receptions" icon={BookOpen} description="Receptions marchandises, bons de livraison, litiges." />;
}