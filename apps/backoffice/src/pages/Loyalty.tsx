import { PageStub } from '../components/PageStub';
import { Heart } from 'lucide-react';

export function LoyaltyPage() {
  return <PageStub title="Fidelite" icon={Heart} description="Points, recompenses, portail client." />;
}