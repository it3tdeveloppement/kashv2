import { PageStub } from '../components/PageStub';
import { ChefHat } from 'lucide-react';

export function RecipesPage() {
  return <PageStub title="Fiches techniques" icon={ChefHat} description="Recettes, couts matieres, ingredients." />;
}