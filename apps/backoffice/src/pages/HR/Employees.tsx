import { PageStub } from '../../components/PageStub';
import { UserCheck } from 'lucide-react';

export function HrEmployeesPage() {
  return <PageStub title="Employes" icon={UserCheck} description="Fiches employes, contrats, horaires." />;
}