import { PageStub } from '../../components/PageStub';
import { Clock } from 'lucide-react';

export function HrTimesheetsPage() {
  return <PageStub title="Pointage" icon={Clock} description="Saisies de temps, absences, demandes de conge." />;
}