import { PageStub } from '../components/PageStub';
import { Users } from 'lucide-react';

export function CustomersPage() {
  return <PageStub title="Clients" icon={Users} description="Base clients, historique achats, segmentation." />;
}