import { PageStub } from '../components/PageStub';
import { Activity } from 'lucide-react';

export function MonitoringPage() {
  return <PageStub title="Monitoring" icon={Activity} description="Evenements systeme, transactions fantomes, sante materiel." />;
}