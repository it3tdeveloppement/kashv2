import { PageStub } from '../components/PageStub';
import { FileText } from 'lucide-react';

export function BillingPage() {
  return <PageStub title="Facturation" icon={FileText} description="Factures, avoirs, proformas, acomptes, royalties." />;
}