import { PageStub } from "../components/PageStub";
import { Settings } from "lucide-react";

export function SettingsPage() {
  return <PageStub title="Paramètres" icon={Settings} description="Configuration du tenant, branding, paiements, matériel, modules." />;
}
